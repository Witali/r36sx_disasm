This directory is the default Pico-286 host drive.

When DOS runs MAPDRIVE.COM, it is exposed as drive H: through the emulator's
network redirector. You can change the directory with host_drive_path in
pico_286.conf.
